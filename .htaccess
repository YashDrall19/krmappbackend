# Enable WSGI app on cPanel Python application
PassengerEnabled on

# Static files settings (adjust as needed)
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^static/(.*)$ static/$1 [L]
</IfModule>
